i = 0;
max = 0;
old_max = 0;
while ( i <= 20 ) {
  original_a = nondet(); 
  a = nondet();
  assume((original_a - 2) <= a);
  assume(a <= (original_a + 2));
  old_max = max;
  if (a < old_max) { 
    max = a; 
  } else {  skip; };
  i = i + 1;
}
